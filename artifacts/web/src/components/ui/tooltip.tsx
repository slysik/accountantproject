/**
 * TooltipProvider stub — in a full build this would wrap the app with
 * a radix-ui tooltip context (e.g., @radix-ui/react-tooltip).
 */
import * as React from "react";

export function TooltipProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

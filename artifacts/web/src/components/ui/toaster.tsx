/**
 * Toaster stub — in a full build this would be a toast notification system
 * (e.g., shadcn/ui Toaster backed by sonner or react-hot-toast).
 */
export function Toaster() {
  return <div id="toaster" />;
}
